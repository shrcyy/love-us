# 💕 两人私密纪念网站

一个温暖治愈的纯静态网站，为情侣/伴侣打造专属的二人秘密花园。无需服务器，无需数据库，所有数据存储在浏览器的 localStorage 中，支持公网免费部署。

## 功能介绍

| 页面 | 功能 |
|------|------|
| **首页** | 空间创建/切换、纪念日倒计时、最近动态、快速导航入口 |
| **时间线日记** | 按时间记录日常点滴，支持搜索筛选、心情标记 |
| **纪念日** | 重要日子管理，自动倒计时提醒 |
| **相册墙** | 照片上传管理，标签分类筛选，点击放大查看 |
| **愿望清单** | 共同目标记录，完成状态勾选 |
| **留言板** | 互相留言，数据本地持久化 |
| **我们的地图** | 基于 Leaflet.js + OpenStreetMap 的交互地图，标记去过的地点、打卡记录 |
| **关于我们** | 个人简介、故事时间轴、爱好标签云、数据统计、分享空间链接、数据导入导出 |

### 特色

- **多情侣独立空间**：通过 URL hash（如 `#ourstory`）创建独立的命名空间，不同情侣数据完全隔离
- **空间分享**：复制链接发给对象，TA 打开即进入同一空间
- **数据导入导出**：支持 JSON 格式导出备份和导入恢复，跨设备迁移数据
- **全站可编辑**：所有内容支持增删改，数据通过 localStorage 持久化
- **温暖治愈系风格**：奶油色/暖粉配色，花瓣飘落动画，完全响应式设计
- **零成本部署**：纯静态文件，可部署到 Vercel / Netlify / GitHub Pages 等平台

## 如何使用

### 1. 创建你们的空间

打开网站首页，点击"创建我们的空间"，输入你们的名字，系统会生成唯一空间标识（URL hash）。记下这个链接，发给你的另一半。

### 2. 添加内容

点击每个页面右下角的粉色"+"按钮，填写表单添加：
- 设置起始日期（在一起的纪念日）
- 在时间线写下第一篇日记
- 上传第一张合照到相册
- 添加重要纪念日
- 在地图上标记一起去过的地方

### 3. 分享给另一半

进入"关于我们"页面的"分享 & 数据管理"区域，点击"复制链接"发给对方。TA 打开链接后进入同一个空间，所有内容自动同步（基于 localStorage）。

## 如何部署到 Vercel

Vercel 提供免费的一键部署，支持自定义域名和 HTTPS。

### 第一步：准备代码

将整个 `love-us/` 目录上传到你的 GitHub 仓库（公开或私有均可）。

### 第二步：导入项目到 Vercel

1. 访问 [vercel.com](https://vercel.com) 并用 GitHub 账号注册/登录
2. 点击「New Project」
3. 导入你的 GitHub 仓库
4. Vercel 会自动检测到 `vercel.json` 配置
5. 直接点击「Deploy」即可

### 第三步：分享你的网站

部署完成后 Vercel 会分配一个域名（如 `https://your-project.vercel.app`）。将链接分享给你的另一半即可。

### 部署到其他平台

- **GitHub Pages**：将 `love-us/` 目录内容推送到 `gh-pages` 分支，在仓库 Settings 中启用 Pages
- **Netlify**：拖拽 `love-us/` 文件夹到 Netlify 面板即可
- **Cloudflare Pages**：连接 GitHub 仓库，构建输出目录设为 `.`

## 分享给其他情侣

1. 维护者部署一次到公网，所有人共用同一个域名
2. 每对情侣在首页创建自己的空间，获得唯一 hash 链接
3. 各自的数据通过本地 localStorage 完全隔离

## 技术栈

- 纯 HTML/CSS/JavaScript（零依赖框架）
- Leaflet.js（地图，CDN 引入）
- localStorage 数据持久化
- Intersection Observer 渐入动画
- 完全响应式设计（480/768/1024/1400px 断点）

## 项目结构

```
love-us/
├── index.html          # 首页
├── timeline.html       # 时间线日记
├── anniversaries.html  # 纪念日
├── gallery.html        # 相册墙
├── wishlist.html       # 愿望清单
├── messages.html       # 留言板
├── map.html            # 我们的地图
├── about.html          # 关于我们
├── css/
│   └── style.css       # 全局样式
├── js/
│   ├── data.js         # 数据层（空间管理、导入导出、存储引擎）
│   └── main.js         # 全局 UI 工具（导航、模态框、卡片操作、空间管理 UI）
├── vercel.json         # Vercel 部署配置
├── .gitignore
└── README.md
```